<h1 class="page-header">Edit Contact</h1>

<%= simple_form_for @person, url: contact_path(@person), html: {class: 'form-horizontal'} do |f| %>
  <%= render 'layouts/shared/error_messages', object: @person %>
  <%= render 'form', :f => f %>
  
  <%= form_actions do %>
    <%= f.button :submit, 'Save' %>
    <%= link_to 'Cancel', contacts.contacts_path, class: 'btn btn-link' %>
  <% end %>
<% end %>